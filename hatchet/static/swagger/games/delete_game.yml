summary: delete a Game.
tags:
  - games
parameters:
  - in: path
    name: game_id
    type: integer
responses:
  204:
    description: Game deleted
  404:
    description: game does not exist
    schema:
      $ref: "#/definitions/Error"

