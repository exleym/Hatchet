summary: get division by id.
description: "get a Division by its primary identifier"
tags:
  - divisions
parameters:
  - in: path
    name: division_id
    type: integer
responses:
  200:
    description: a division object
    schema:
      $ref: "#/definitions/Division"
  404:
    description: division not found
    schema:
      $ref: "#/definitions/Error"

