tags:
  - conferences
produces:
  - application/json
parameters:
  - in: path
    name: conference_id
    description: delete conference identified by conference_id
    required: true
responses:
  204:
    description: conference deleted
