tags:
  - conferences
consumes:
  - application/json
produces:
  - application/json
parameters:
  - in: "body"
    name: body
    description: "Conference to add to database"
    required: true
    schema:
      $ref: "#/definitions/Conference"
responses:
  201:
    description: Conference created
    schema:
      $ref: '#/definitions/Conference'
  405:
    description: Invalid input
