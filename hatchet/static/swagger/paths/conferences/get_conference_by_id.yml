tags:
  - conferences
produces:
  - application/json
parameters:
  - in: path
    name: conference_id
    description: "get conference by id"
    required: true
responses:
  200:
    description: the conference matching the provided conference_id
    schema:
      $ref: '#/definitions/Conference'
  404:
    description: missing resource / resource not found
