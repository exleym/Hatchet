tags:
  - conferences
produces:
  - application/json
parameters:
  - in: query
    name: code
    description: "get conference by code"
    required: false
responses:
  200:
    description: array of all conferences or conferences matching query params
    schema:
      type: array
      items:
        $ref: '#/definitions/Conference'
