tags:
  - conferences
produces:
  - application/json
parameters:
  - in: path
    name: conference_id
    description: "get conference by id"
    required: true
responses:
  200:
    description: array of divisions belonging to conference
    schema:
      type: array
      items:
        $ref: '#/definitions/Division'
