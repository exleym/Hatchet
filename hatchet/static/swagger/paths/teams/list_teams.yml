tags:
  - teams
produces:
  - application/json
parameters:
  - in: query
    name: shortName
    description: get team by shortName
    required: false
responses:
  200:
    description: array of all teams or teams matching query params
    schema:
      type: array
      items:
        $ref: '#/definitions/Team'
