tags:
  - teams
consumes:
  - application/json
produces:
  - application/json
parameters:
  - in: "body"
    name: body
    description: "Team to add to database"
    required: true
    schema:
      $ref: "#/definitions/Team"
responses:
  201:
    description: Team created
    schema:
      $ref: '#/definitions/Team'
  405:
    description: Invalid input
