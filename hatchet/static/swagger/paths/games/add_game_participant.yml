tags:
  - games
consumes:
  - application/json
produces:
  - application/json
parameters:
  - in: body
    name: body
    description: Associate participant with a game"
    required: true
    schema:
      $ref: "#/definitions/GameParticipant"
responses:
  201:
    description: Game-Participant association created
    schema:
      $ref: '#/definitions/Game'
  405:
    description: Invalid input
