tags:
  - games
produces:
  - application/json
parameters:
  - in: path
    name: game_id
    description: delete game identified by game_id
    required: true
responses:
  204:
    description: game deleted
