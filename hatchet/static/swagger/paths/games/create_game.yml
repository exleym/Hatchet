tags:
  - games
consumes:
  - application/json
produces:
  - application/json
parameters:
  - in: body
    name: body
    description: Game to add to database"
    required: true
    schema:
      $ref: "#/definitions/Game"
responses:
  201:
    description: Game created
    schema:
      $ref: '#/definitions/Game'
  405:
    description: Invalid input
