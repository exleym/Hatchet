tags:
  - games
produces:
  - application/json
parameters:
  - in: path
    name: game_id
    description: the game id to query
    required: true
responses:
  200:
    description: a list of participants for a specific game
    schema:
      type: array
      items:
        $ref: '#/definitions/GameParticipant'
