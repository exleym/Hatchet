tags:
  - divisions
produces:
  - application/json
parameters:
  - in: path
    name: division_id
    description: "the division id to query"
    required: true
responses:
  200:
    description: a list of teams belonging to division
    schema:
      type: array
      items:
        $ref: '#/definitions/Team'
