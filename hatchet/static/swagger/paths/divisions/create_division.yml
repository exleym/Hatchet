tags:
  - divisions
consumes:
  - application/json
produces:
  - application/json
parameters:
  - in: "body"
    name: body
    description: "Division to add to database"
    required: true
    schema:
      $ref: "#/definitions/Division"
responses:
  201:
    description: Division created
    schema:
      $ref: '#/definitions/Division'
  405:
    description: Invalid input
