summary: delete a Team.
tags:
  - teams
parameters:
  - in: path
    name: team_id
    type: integer
responses:
  204:
    description: Team deleted
  404:
    description: team does not exist
    schema:
      $ref: "#/definitions/Error"

