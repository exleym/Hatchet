summary: delete a Stadium.
tags:
  - stadiums
parameters:
  - in: path
    name: stadium_id
    type: integer
responses:
  204:
    description: Stadium deleted
  404:
    description: stadium does not exist
    schema:
      $ref: "#/definitions/Error"

