Core Data Model
===============
We model the vast majority of our data within a relational database and
expose it publicly via a Python API. This section will detail the data
model itself and the public schema exposed via the API.

The SQL Data Schema
-------------------

.. image:: ../../static/schema-diagram.png